if key < list(mid)
return binary (list, -,-)
simple_work_code(n,a,b)
use //

# lets say n = 9, a = 2 , b = 3

                n = 9
        (n/3)=3        (n/3)=3
  1            1      1            1

return code of work (n//b, a, b)+ f(n)
need to find the stopping point

# to stop the code
if n//b == 1
return 1

if n > b  return 0 bc n//b = 0
# so end value is gonna be 1 or 0